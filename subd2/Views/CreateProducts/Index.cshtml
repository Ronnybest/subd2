@model IEnumerable<subd2.Models.CreateProduct>

@{
    ViewData["Title"] = "Index";
}

<h1>Last created products history</h1>

<p>
    <a asp-action="Create">Create products</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Product
                
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Count)
                
            </th>
            <th>
                Employee
                @*@Html.DisplayNameFor(model => model.EmployeeNavigation)*@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.ProductNavigation.Name)
                
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Count)
                
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeNavigation.FullName)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
